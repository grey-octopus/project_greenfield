import { getRelatedProducts } from "../src/related_prod_your_outfit/actions/Related_Products";

// getRelatedProducts:
// it should accept the id of the product from the page
// it should return an function
// that func(thunk) should return a promise
// resolved promise should have a
// type: "GET_RELATED_PRODUCTS"
// relatedProducts =>
// relatedProducts should be an array
// each index of the array should contain
// id num
// category string
// name string
// price num
// rating num || null

// create mock result and test?

// does it get the correct information for the product ID
